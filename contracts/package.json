{
  "name": "droprank-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DropRank soulbound badge ERC-721 (Base)",
  "scripts": {
    "build": "hardhat build",
    "test": "hardhat test solidity",
    "deploy:sepolia": "hardhat run script/deploy.ts --network baseSepolia",
    "deploy:base": "hardhat run script/deploy.ts --network base",
    "deploy:sepolia:checkin": "hardhat run script/deploy-checkin.ts --network baseSepolia",
    "deploy:base:checkin": "hardhat run script/deploy-checkin.ts --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "@types/node": "^22.19.19",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.0.0",
    "typescript": "~5.6",
    "viem": "^2.21.0"
  }
}
