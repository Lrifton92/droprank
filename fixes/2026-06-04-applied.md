# DropRank — Rapport remédiation appliquée (2026-06-04)

Branche : master. 12 findings reçus (2 audits cybersec). 12/12 fixés, 0 reporté, 0 WONTFIX.

## Résultats de test (sortie réelle)

| Suite | Avant | Après |
|-------|-------|-------|
| vitest (`npx vitest run`) | 38 ✅ | **42 ✅** (+4 clientIp) |
| build (`npm run build`) | vert | **vert** (Next 15.5.19) |
| contrat (`cd contracts; npm test`) | 19 ✅ | **23 ✅** (+4) |

> Note runner contrat : Hardhat 3 (`hardhat test solidity`) exécute les `.t.sol`.
> Commande = `cd contracts; npm test` (le `npx hardhat test` nu échoue HHE22 — install locale requise, faite).

## Tableau fix → statut → commit → root cause / safeguard

| # | Fix | Statut | Commit | Root cause → safeguard |
|---|-----|--------|--------|------------------------|
| FIX-1 | Bump Next 15.3.4→15.5.19 (CVE) | ✅ | `8110cef` | RC : version dans la plage CVE content-injection + cache-key. Safeguard : pinné exact (pas de caret) + plugins eslint alignés. **Side-effect** : Next 15.5 typecheck plus strict → cassait `withValidManifest(minikitConfig)` (prop `baseBuilder` hors type union OnchainKit) → corrigé par cast typé au call-site (champ runtime conservé). |
| FIX-2 | chainId configurable + refus 0x000 | ✅ | `ecc8ab2` | RC : 8453 hardcodé → impossible de viser Sepolia. Safeguard : `BADGE_CHAIN_ID` unique partagé route+MintButton (cohérence signature↔write garantie) ; sign-score 503 si contrat 0x000. |
| FIX-3 | Rate-limit distribué Upstash + fallback | ✅ | `6d453c4` | RC : limiter in-memory = 0 effet en serverless. Safeguard : Upstash sliding-window quand configuré, fallback in-memory sinon, **fail-open** sur erreur Redis (route jamais down). |
| FIX-3b | clientIp trusted | ✅ | `6d453c4` | RC : `XFF[0]` spoofable. Safeguard : `x-vercel-forwarded-for` puis dernier hop XFF ; +4 tests unitaires. |
| FIX-4 | Lock renounceOwnership | ✅ | `c11e8af` / test `7f9f140` | RC : renonciation accidentelle = perte définitive setSigner. Safeguard : override `revert RenounceDisabled` + test. |
| FIX-5 | TTL deadline 10→3 min | ✅ | `8c04ebb` | RC : fenêtre replay trop large. Safeguard : `ATTESTATION_TTL_SECONDS = 3*60`, suffisant (mint one-shot, refresh re-signe). |
| FIX-6 | burn ownership guard | ✅ | `c11e8af` / test `7f9f140` | RC : `burn` ne vérifiait pas l'appartenance explicitement. Safeguard : `require tokenOf[msg.sender]==tokenId` (NotOwner) + test BurnWrongTokenId. |
| FIX-7 | Commentaire anti-injection tokenURI | ✅ | `c11e8af` | RC : prévention drift futur. Safeguard : note SECURITY au-dessus de tokenURI. |
| FIX-8 | Trous tests contrat | ✅ | `7f9f140` | Ajout : score==100 (MintAtMaxScore), setSigner(0) revert, re-mint après burn (déjà couvert par test_CanMintAgainAfterBurn). |
| FIX-9 | OG appel direct scoreAddress + cache | ✅ | `8400d30` | RC : self-fetch HTTP `/api/score` doublait le quota Blockscout. Safeguard : `scoreAddress()` in-process + `s-maxage=3600, stale-while-revalidate`. Edge-compat vérifiée (build). |
| FIX-10 | Erreurs génériques | ✅ | `23468a8` | RC : `e.message` upstream fuité au client. Safeguard : "Upstream unavailable"/"Invalid address" côté client, `console.error` détaillé serveur. |
| FIX-11 | Headers sécurité + CSP report-only | ✅ | `b96c87d` | RC : aucun header durci. Safeguard : HSTS/nosniff/referrer/permissions + CSP **report-only** (ne casse pas OnchainKit), frame-ancestors Farcaster/Base, wss: WC, pas d'unsafe-eval, pas de X-Frame-Options DENY. |
| FIX-12 | .gitignore .env* | ✅ | `872452e` | RC : `.env` seul → `.env.local` etc. trackables. Safeguard : `.env*` + `!.env.example` (vérifié check-ignore). |

## Findings challengés (vérifiés avant application)

- FIX-2 côté MintButton : le placeholder 0x000 était **déjà** traité (bouton "Badge soon"). Fix appliqué uniquement côté route sign-score (qui ne le traitait pas). Pas de double travail.
- FIX-8 "re-mint après burn" : test **déjà existant** (`test_CanMintAgainAfterBurn`). N'ai pas dupliqué.

## Side-effects / décisions

- **Next 15.5 typecheck** : a forcé le cast manifest (FIX-1). Documenté, champ runtime intact.
- **OnchainKit 1.1.2 inchangé** : compatible avec Next 15.5.19, build vert. Pas de bump OnchainKit nécessaire.
- **basescan.org hardcodé** dans `MintButton.onMint` (lien tx succès) : sur Sepolia ce lien pointerait vers le mauvais explorer (devrait être sepolia.basescan.org). **Hors-scope FIX-2** (qui ne couvrait que le chainId de signature/write). Mentionné, NON modifié — à traiter avant le déploiement Sepolia si le lien tx doit être correct en testnet.

## Hors-scope / non touché (volontaire)

- **Changements working-tree pré-existants NON commités** (pas de moi) : `lib/quests.ts`, `lib/quests.test.ts`, `lib/contracts-registry.ts` (remplacement quest Zora→Morpho), `app/_components/presentation.ts`, `docs/specs/2026-06-03-droprank-design.md`. Laissés intacts — chantier séparé de Soufian.
- Warning taille contrat de TEST (49KB) : c'est le contrat de test (helpers base64), pas le badge. Non déployable concern.
- `npm audit` : reste un transitif `postcss` via next (faux remède = downgrade next@9, ignoré) et vulns dev hardhat/foundry-std. Hors scope applicatif.

## Vérification subagent

⚠️ `assistant-fixeur` non dispatché : le tool Task n'est pas disponible dans cet environnement d'exécution. J'ai exécuté moi-même les 3 suites + build à neuf (`.next` purgé) après les fixes structurels. **À faire par l'orchestrateur** : lancer `assistant-fixeur` pour smoke runtime (npm run dev + hit /api/score, /api/og, /api/sign-score avec/sans Upstash) avant déploiement.

## Actions humaines requises (Soufian)

1. Définir `NEXT_PUBLIC_CHAIN_ID=84532` dans l'env Vercel pour le déploiement Base Sepolia (puis 8453 pour mainnet).
2. Provisionner Upstash (UPSTASH_REDIS_REST_URL/TOKEN) en prod pour que le rate-limit soit réellement distribué (sinon fallback in-memory = quasi-inopérant en serverless).
3. Après observation du CSP report-only en prod (rapports de violation), basculer `Content-Security-Policy-Report-Only` → `Content-Security-Policy` pour l'enforcement.
4. Décider si le lien tx basescan doit gérer Sepolia (cf. side-effect ci-dessus) avant le testnet.
