# DropRank — Triage remédiation post-audit (2026-06-04)

Source : 2 audits cybersec. Décisions Soufian actées : OWNER = EOA basename (pas multisig) + désactiver renounceOwnership ; déploiement Base Sepolia d'abord puis mainnet (chainId configurable).

Baseline avant travaux : 38 vitest verts · 19 tests contrat verts · build vert (master).

Tri par blast radius décroissant (= ordre d'application imposé par le brief).

| # | Sévérité | Finding | Statut | Effort | Blast radius |
|---|----------|---------|--------|--------|--------------|
| FIX-1 | CRITICAL | Next.js 15.3.4 vulnérable (content injection + cache key confusion) → bump | 🚨 NOW | 30 | Toute l'app |
| FIX-2 | HIGH | chainId hardcodé (8453) → `NEXT_PUBLIC_CHAIN_ID` configurable + refus signer si contrat absent/0x000 | ⚡ TODAY | 30 | sign-score + mint |
| FIX-3 | HIGH | RateLimiter in-memory inopérant en serverless → Upstash distribué + fallback | ⚡ TODAY | 60 | 4 routes API |
| FIX-3b | HIGH | clientIp() spoofable (XFF[0]) → header de confiance Vercel | ⚡ TODAY | 15 | rate-limit |
| FIX-11 | HIGH | Pas de headers sécurité Next (HSTS, nosniff, CSP report-only…) | ⚡ TODAY | 30 | Toute l'app |
| FIX-4 | décision | Lock ownership : renounceOwnership() → revert | 📅 (contrat) | 15 | Contrat (perte clé) |
| FIX-9 | MEDIUM | OG self-fetch HTTP → appel direct scoreAddress() + cache-control | 📅 | 30 | Route OG (quota) |
| FIX-5 | MEDIUM | TTL deadline 10min → 3min (anti-replay temporel) | 📅 | 5 | sign-score |
| FIX-10 | MEDIUM | Erreurs BlockscoutError brutes au client → génériques + log serveur | 📅 | 15 | 3 routes |
| FIX-6 | LOW | burn : require(tokenOf[msg.sender]==tokenId) défensif | 📅 (contrat) | 15 | Contrat |
| FIX-7 | LOW | Commentaire anti-injection au-dessus de _svg/tokenURI | 📅 | 5 | Doc contrat |
| FIX-8 | INFO | Combler trous tests contrat (score==100, setSigner(0), re-mint) | 📅 | 30 | Tests |
| FIX-12 | INFO | contracts/.gitignore : `.env` → `.env*` (garde !.env.example) | 📅 | 5 | Repo |

## Notes de vérification (findings challengés avant application)

- **FIX-2** : `app/score/MintButton.tsx` traite déjà le placeholder 0x000 (ligne 13-18, `CONTRACT = null` → bouton "Badge soon"). La route sign-score, elle, valide `isAddress` mais PAS le 0x000 explicite → ajout nécessaire côté route uniquement.
- **FIX-3** : `@upstash/ratelimit` n'est PAS installé (le brief supposait que oui). Seul `@upstash/redis` l'est. → install requise.
- **FIX-9** : la route OG est `runtime = "edge"`. `scoreAddress()` ne touche pas Upstash (le percentile n'est appelé que dans /api/score), uniquement `fetch` Blockscout → compatible edge. OK pour appel direct.
- **Contrat** : test runner réel = Hardhat 3 (`hardhat test solidity`) exécutant les `.t.sol` style Foundry. Commande de vérif : `cd contracts; npm test` (pas `npx hardhat test` bare → HHE22).

## Hors-scope / non fixés

- Warning taille du contrat de TEST (49 KB) au build : c'est le contrat de test (helpers base64), pas le badge déployable. Non bloquant, pas touché.
- `npm audit` contracts : 17 vulns (deps dev hardhat/foundry-std). Hors scope audit applicatif, deps de dev uniquement.
